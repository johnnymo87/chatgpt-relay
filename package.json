{
  "name": "chatgpt-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "ask-question": "./src/cli.js",
    "ask-question-server": "./src/server.js",
    "ask-question-login": "./src/login.js"
  },
  "scripts": {
    "server": "node src/server.js",
    "server:log": "mkdir -p ~/.local/state/chatgpt-relay && node src/server.js 2>&1 | tee ~/.local/state/chatgpt-relay/daemon.log",
    "test": "node --test src/*.test.js"
  },
  "dependencies": {
    "playwright": "^1.56.0",
    "undici": "^7.18.2"
  }
}
